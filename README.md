# notas 
Este es mi primer programa con NODE

```
Options:
    --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Es la base con la cual se crea la tabla de multiplicar
                                                            [número] [requerido]
  -l, --listar   Muestra la tabla que se ejecuta     [booleano] [defecto: false]
  -h, --hasta    limite hasta donde quiero que llegue la tabla          [número]
```